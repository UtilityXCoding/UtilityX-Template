{
  "version": "v1.0.0",
  "date": "2025-07-29",
  "changes": [
    {
      "type": "feature",
      "title": "Add GitHub Actions workflow for automated builds and releases",
      "description": "Introduced CI pipeline triggered by Git tag pushes (v*). Builds the plugin using Gradle, uploads artifacts to GitHub Releases."
    },
    {
      "type": "feature",
      "title": "Add changelog generation using release-changelog-builder-action",
      "description": "Automatically generates changelogs based on PR labels like `feature`, `bug`, and `chore`."
    }
  ]
}
